# dart run package_rename --path="package_rename_config.yaml"
package_rename_config:
  android:
    app_name: Bucket
    package_name: com.bucket_anikitgrover
    override_old_package: # (Optional) (String) Use this to delete the old folder structure of MainActivity or to use the existing code with the new package name
    lang: # (Optional) (String) The android development language {kotlin(default) or java}

  ios:
    app_name: Bucket # (String) The display name of the ios app
    bundle_name: com.bucket_anikitgrover # (String) The bundle name of the ios app
    package_name: com.bucket_anikitgrover # (String) The product bundle identifier of the ios app
  web:
    app_name: Bucket # (String) The title and display name of the web app and PWA
    description: Bucket # (String) The description of the web app and PWA
